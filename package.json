{
  "devDependencies": {
    "http-server": "^0.10.0",
    "jquery": "^3.2.1",
    "npm-run-all": "^4.1.1",
    "vue": "^2.4.2"
  },
  "scripts": {
    "dev": "npm-run-all deploy serve",
    "serve": "http-server public -c 1 -o",
    "deploy": "./deploy"
  }
}
